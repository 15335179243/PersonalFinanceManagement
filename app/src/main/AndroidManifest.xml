<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chumu.jianzhimao">

    <application
        android:name=".RootApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="ExtraText">
        <activity android:name=".ui.activity.login.Main2Activity">



        </activity>
        <activity android:name=".ui.activity.login.ForgetPasswordActivity" />
        <activity android:name=".ui.activity.MainMessageActivity" />
        <activity
            android:name=".ui.activity.HomeActivity"
            android:label="@string/home" />
        <activity android:name=".ui.activity.SplashActivity">

        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/address" />
        <activity android:name=".ui.activity.SettingActivity" />
        <activity android:name=".ui.activity.login.ChangePasswordActivity" />
        <activity android:name=".ui.activity.login.RegisterAndPhoneLoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>